function calcDelta() {

    a = 1;

    b = -4;

    c = 3;

    var delta = b**2 - 4*a*c;

    document.getElementById("delta").innerHTML = delta;

}

// add the conditions for the delta => > 0, = 0, <0