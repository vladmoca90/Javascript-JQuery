//The parameter is an array with 3 elements that present the sides of a triangle
//Return the perimeter of the triangle

function calculatePerimeter(array) {

    return array[0] + array[1] + array[2];

}